<h1>Clientes</h1>

<table>
  <tr>
    <th>Código</th>
    <th>Nome</th>
    <th>Telefone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clientes.each do |cliente| %>
  <tr>
    <td><%= cliente.codigo %></td>
    <td><%= cliente.nome %></td>
    <td><%= cliente.telefone %></td>
    <td class="acao"><%= link_to image_tag('exibir.png'), cliente %></td>
    <td class="acao"><%= link_to image_tag('editar.png'), edit_cliente_path(cliente) %></td>
    <td class="acao"><%= link_to image_tag('excluir.png'), cliente, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />


<div class="actions">
    <%= link_to 'Novo', new_cliente_path %>
</div>
